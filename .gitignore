# Python virtual environment (large, recreate from README_LABS.md)
.venv/

# Datasets (large; download per README_LABS.md). GTSRB pickles ~150 MB.
data/

# Python bytecode / Jupyter checkpoints
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# macOS
.DS_Store

# Instructor manuals — keep LOCAL only, never publish (they contain answers/Q&A)
Lab1_Instructor_Manual.md
Lab2_Instructor_Manual.md
Instructor_Manual_Combined.md

# NOTE: artifacts/ IS tracked on purpose — the pre-computed patch + backdoor
# model are small (~4 MB) and let you restore a known-good demo state.
